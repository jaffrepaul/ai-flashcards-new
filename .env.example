# Database (will be set automatically by: npx neondb -y)
DATABASE_URL=

# Auth - Generate a secure secret with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# This is used to encrypt user session cookies
JWT_SECRET=

# Optional: AI SDK (you'll add this later)
# OPENAI_API_KEY=
